<!-- sidebar: style can be found in sidebar.less -->
<section class="sidebar">

    <!-- Sidebar user panel (optional) -->
    <div class="user-panel">
        <div class="pull-left image">
            <img src="{{ asset('assets/AdminLTE/dist/img/user2-160x160.jpg') }}" class="img-circle" alt="User Image">
        </div>
        <div class="pull-left info">
            <p>{{ app.user.name }}</p>
            <!-- Status -->
            <a href="#"><i class="fa fa-circle text-success"></i> Online</a>
        </div>
    </div>

    <!-- Sidebar Menu -->
    <ul class="sidebar-menu">
        <li class="header">HEADER</li>
        {% if is_granted('ROLE_EDITOR') or is_granted('ROLE_ADMIN') %}
            <li><a href="{{ path('task_index') }}"><i class="fa fa-bookmark-o"></i> All Tasks</a></li>
        {% endif %}
        <li><a href="{{ path('group_task_index') }}"><i class="fa fa-bookmark-o"></i> Group Tasks</a></li>
        <li><a href="{{ path('my_task_index') }}"><i class="fa fa-bookmark-o"></i> My Tasks</a></li>
    </ul>
    <!-- /.sidebar-menu -->

</section>
<!-- /.sidebar -->